<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd"
            schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>mycobot_csp</name>
  <version>0.1.0</version>
  <description>
    ros2_control SystemInterface plugin driving Laifual L70I-E-100-BF in
    CiA 402 CSP mode (Cyclic Synchronous Position, mode 8). Position
    command interface; the drive runs the position loop. Sibling of the
    verified mycobot_csv (CSV/velocity) baseline.
  </description>

  <maintainer email="zafarmunzir@gmail.com">Munzir</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <depend>hardware_interface</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>

  <export>
    <build_type>ament_cmake</build_type>
    <hardware_interface plugin="${prefix}/mycobot_csp_plugin.xml"/>
  </export>
</package>
